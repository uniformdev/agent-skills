Integrate Uniform CMS into this Next.js App Router project so that pages managed in Uniform are rendered by the app.

Requirements:
- Any path managed in the Uniform project (for example /about) must resolve and render its composition
- Map the Uniform component type `hero` to a React component that renders its `title` text parameter inside an `<h1>`
- Keep the existing home page working
- Uniform environment variables are already configured in `.env`

Do not ask questions — make reasonable decisions and complete the integration.
