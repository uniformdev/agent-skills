// Barrel file: importing a component module is what runs its
// registerUniformComponent() call. A component that is never imported here is
// never registered, and renders as "not implemented".
import "./Page";
